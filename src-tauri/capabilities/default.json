{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for ViewDocx",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "core:webview:default",
    "dialog:default",
    "allow-app-commands"
  ]
}
